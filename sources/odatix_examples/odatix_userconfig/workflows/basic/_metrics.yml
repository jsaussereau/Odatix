##############################################
# Metrics for this workflow
##############################################

metrics:
  word:
    type: regex
    settings:
      file: output.txt
      pattern: "text: (.*)"
      group_id: 1

  letters:
    type: regex
    settings:
      file: output.txt
      pattern: "letters: ([0-9]+)"
      group_id: 1
    format: "%.0f"
